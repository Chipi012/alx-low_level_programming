#include "main.h"
/**
 * mul - Mulitplication of two int
 * @x: The first num to multiply
 * @y: The second num to multiply with first
 * Return: The result of multiply between x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
